blob_top_vec_.push-ack(blob_top_alvel_);
//Create the leveldb
filenam + tmpnam(NULL0;/get temp name 
LOG(ERROR)<<"Using temporyaty leveldb"<<filename;
Log(INFO)<<"Using tempory leveldb"<<filename;
leveldb::DB*db;
leveldb:options options;
options.error_if_exists =true;
